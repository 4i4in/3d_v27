function exe_grab_the_world()
{
	/*
	var new_vertex_buffer = vertex_create_buffer();
	
	vertex_begin(new_vertex_buffer, vformat);
	
	for (var i = 0; i < vertex_get_number(vertex_buffer); i++) 
		{
		    var pos = vertex_get(vertex_buffer, vformat, i, "position");
		    var normal = vertex_get(vertex_buffer, vformat, i, "normal");
		    var texcoord = vertex_get(vertex_buffer, vformat, i, "texcoord");

		    vertex_position(pos[0], pos[1], pos[2]);
		    vertex_normal(normal[0], normal[1], normal[2]);
		    vertex_texcoord(texcoord[0], texcoord[1]);
		    vertex_add(new_vertex_buffer);
		}
	vertex_end();
	*/
}